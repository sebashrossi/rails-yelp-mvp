<%= simple_form_for restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category, hint: 'Accepted categories are : Chinese, Italian, Japanese, French, Belgian' %>
  <%= f.button :submit, class: 'btn btn-outline-success' %>
<% end %>
